apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-prod
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gitops-app
  template:
    metadata:
      labels:
        app: gitops-app
    spec:
      containers:
      - name: nginx
        image: nginx:latest